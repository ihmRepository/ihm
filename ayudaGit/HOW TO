comandos basicos:

1) clonar este repositorio en tu ordenador:
	git clone git://github.com/ihmRepository/ihm.git

2) eliminar un archivo o carpeta del repositorio:
	//si no esta clonado -> clonar en tu pc
	//acceder a la carpeta (cd ihm)
	//git rm -r nombreDirectorio (-r para carpetas..y sin el archivos normales)
	//gi commit -m "he borrado esto ..."
	//git remote add nombreConexion https://github.com/ihmRepository/ihm
	//git push nombreConexion master (master en caso de k sea esa la rama...) 
	
3) agregar:
	//git add nombreArchivo
	//git commit -m "he agregado..."
	//git remote ...
	//git push ...

notas: a veces el git rm -r no es suficiente para borrar algun directorio: usar:
	//git rm --cached -r nombreDirectorio (esto borrara al archivo del github pero no de nuestro repositorio local) -> lo eliminamos con rm -r nombredirectorio y listo!

